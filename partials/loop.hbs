{{#get "posts" filter="featured:true{{is "tag"}}+primary_tag:{{tag.slug}}{{/is}}" include="tags" limit="all" as |featured|}}
{{#foreach featured}}
    {{> card}}
{{/foreach}}
{{/get}}

{{#foreach posts}}
    {{^if featured}}
        {{> card}}
    {{/if}}
{{/foreach}}