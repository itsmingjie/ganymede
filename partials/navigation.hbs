{{#foreach navigation}}
<a class="header__link nav__{{slug}}{{#if current}} nav__current{{/if}}" href="{{url absolute="true"}}">{{label}}</a>
{{/foreach}}
<a class="header__link dark__toggle" href="#!" onclick="toggleTheme(true)">🌙</a>
<a class="header__link light__toggle" href="#!" onclick="toggleTheme(false)">🌞</a>